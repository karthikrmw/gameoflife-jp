/** 
	This template file contains a value which will be replaced
    with an environment-specific value.
    The template file is the one that must be added in git

	The generated file will have the same name, excluding the "template" suffix
	The generated file should be ignored by git (since it will change per environment)
**/

module.exports = "@@addteqHomeURL"